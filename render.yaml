services:
  - type: web
    name: valichord-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 300 --workers 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
