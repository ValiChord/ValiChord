# Port of the Holochain app WebSocket interface.
# Not needed when running inside Holochain Launcher (port injected via URL hash).
# For development against a raw conductor: set to whatever port hc sandbox uses.
VITE_HC_PORT=8888
