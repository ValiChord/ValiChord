# `<demo_name>` — ValiChord Attestation v1.1

> This is a suggested skeleton, not a rigid structure. Adapt it where the
> demo's specifics warrant deviation. The two existing demos
> (`mistral_7b_gsm8k_demo`, `inspect_ai_popularity_demo`) follow this
> structure with minor variations.

---

## What this demo is

*3–4 sentences for a casual reader: which harness / model / task, what the demo
does (build bundle → challenge-response → tamper detection), whether a GPU or
download is required to verify, and a clear statement that this is a protocol
demo, not a statistically powered benchmark run.*

---

## What this demonstrates

*Technical feature table: which features of the v1.1 protocol are exercised and
where in the code each is demonstrated.*

| Feature | Where |
|---|---|
| … | … |

---

## Relationship to upstream issues

*Cross-references to GitHub issues, PRs, or proposals that motivated this demo
or that the demo provides evidence for. Omit this section if there are no
relevant upstream references.*

---

## Files

*Table of every file in this demo folder — purpose in one short phrase.*

| File | Purpose |
|---|---|
| … | … |

---

## Reproduction

*Step-by-step instructions. Must include a no-GPU / no-download path using the
committed fixture. Label each step clearly (Step 1 / Step 2 / …).*

### No GPU? Run the demo anyway

*Quickstart using the committed `bundle.json`.*

### Full reproduction

*Steps for generating a real bundle from scratch (eval run → `build_bundle.py`
→ `challenge_response_demo.py`).*

---

## Common failure modes

*Table of symptoms → likely cause → fix. Aim for the 3–5 most common errors a
contributor is likely to hit.*

| Symptom | Likely cause | Fix |
|---|---|---|
| … | … | … |

---

## Honest framing

*Statistical caveats, fixture vs real bundle distinction, scope limits. What
this demo is NOT (not a production adapter, not a statistically significant
result, not a claim about model quality). Required — do not omit.*

---

## Pinned versions

*Table of all component / library versions used in this demo.*

| Component | Version |
|---|---|
| … | … |

---

*This template is a suggested skeleton, not a rigid structure. Future demos should adapt it where the demo's specifics warrant deviation. The two existing demos (mistral_7b_gsm8k_demo, inspect_ai_popularity_demo) follow this structure with minor variations.*
