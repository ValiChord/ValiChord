flask==3.1.0
flask-cors==5.0.0
# 1.x verified against this code 2026-08-24: it uses none of the surface 1.0 removed
# (no temperature/top_p/top_k, with_raw_response, Text Completions, or output_format).
# Upper bound is deliberate — an unbounded pin is how 1.0.0 arrived here unnoticed.
anthropic>=1,<2
gunicorn==22.0.0
# Bounded for the same reason as anthropic above, and now more sharply: with
# --local, litellm is the load-bearing path to the validator models rather than
# a fallback for non-Anthropic keys.
litellm>=1.40.0,<2

# report_to_verdict.py imports scipy.stats at module scope, so importing that
# module - including from its own test file - fails outright without it. It has
# been missing since the module landed; nothing noticed because demo/ had no CI
# until demo.yml was added alongside this line.
scipy>=1.11
