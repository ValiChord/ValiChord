---
# GENERATED-BY-HAND, test-only. Do NOT pack this for production.
# Integrity zome path points at the PRODUCTION build (identical bytes to the
# shipped DNA). Only the coordinator comes from target-test/, where it is
# built with --features test_utils to expose the immutability tripwire hooks.
# Built + packed by ./build-test-dnas.sh into ../workdir-test/.
manifest_version: "0"
name: researcher_repository
integrity:
  network_seed: ~
  properties: ~
  zomes:
    - name: researcher_repository_integrity
      hash: ~
      path: "../../target/wasm32-unknown-unknown/release/researcher_repository_integrity.wasm"
      dependencies: ~
coordinator:
  zomes:
    - name: researcher_repository_coordinator
      hash: ~
      path: "../../target-test/wasm32-unknown-unknown/release/researcher_repository_coordinator.wasm"
      dependencies:
        - name: researcher_repository_integrity
